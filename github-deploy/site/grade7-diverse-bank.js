(()=>{
 const base=window.grade7ImportedQuestion;
 const make=(u,l,t,a,w,h,e)=>{const o=[a,...w].map(String).filter((v,i,x)=>x.indexOf(v)===i),n=Number(a);for(const v of(Number.isFinite(n)?[n+1,n-1,-n,n+2,n*2,0]:['條件不足','以上皆非','無法判斷','兩者相同']).map(String))if(o.length<4&&!o.includes(v))o.push(v);return{u,l,t,o:o.slice(0,4),a:0,h,e,fig:''}};
 const labels={easy:'基礎',medium:'中等',hard:'挑戰'};
 window.grade7ImportedQuestion=(u,l,s)=>{
  const z=Math.abs(Math.floor(s)),k=z%10,n=4+Math.floor(z/10)%17,p=2+Math.floor(z/37)%8;
  const scenes=['個人練習','小組討論','校園任務','生活應用','閱讀判斷','錯誤診斷','圖卡檢核','會考準備'];
  const focuses=['圈出關鍵量','選擇合適算式','逆向檢查答案','辨認常見錯誤','連結文字與符號','說明判斷理由'];
  const tag=`${scenes[Math.floor(z/100)%scenes.length]}・${focuses[Math.floor(z/800)%focuses.length]}｜【${labels[l]}・多元題型】`;
  if(k<3)return base(u,l,Math.floor(z/10)*3+k);
  if(u==='數與數線'){
   if(k===3)return make(u,l,`${tag}下列哪一個數最小？`,-n,[n,-p,0],'負數離原點愈遠，數值愈小。',`−${n} 最小。`);
   if(k===4)return make(u,l,`${tag}|−${n}|＋|${p}−${n+p}| 的值為何？`,2*n,[n,n+p,2*p],'分別算絕對值。',`${n}+${n}=${2*n}。`);
   if(k===5)return make(u,l,`${tag}某點向右移 ${p} 單位後位於 ${n}，原來的位置為何？`,n-p,[n+p,-n,n],'逆推時向左移。',`${n}−${p}=${n-p}。`);
   if(k===6)return make(u,l,`${tag}與 −${n} 距離 ${p} 單位的點中，較大者為何？`,-n+p,[-n-p,n+p,n-p],'左右各有一個位置。',`−${n}+${p}=${-n+p}。`);
   if(k===7)return make(u,l,`${tag}數線上 a<0 且 |a|=${n}，a 為何？`,-n,[n,0,1],'負數條件決定符號。',`a=−${n}。`);
   if(k===8)return make(u,l,`${tag}海拔 ${p} 公尺與海平面下 ${n} 公尺的高度差為何？`,n+p,[n-p,n,p],'跨過海平面要相加。',`${p}−(−${n})=${n+p}。`);
   return make(u,l,`${tag}將 −${n}、${p}、0、−${p} 由小到大排列，第二個數為何？`,-p,[-n,0,p],'先比較兩個負數。',`−${n}<−${p}<0<${p}。`);
  }
  if(u==='整數四則運算'){
   if(k===3)return make(u,l,`${tag}計算 (−${n})＋${p}−(−${p})。`,-n+2*p,[-n,n,-n+p],'減去負數改成加法。',`−${n}+${2*p}=${-n+2*p}。`);
   if(k===4)return make(u,l,`${tag}(−${p})×${n}÷${p} 的值為何？`,-n,[n,-p,n*p],'乘除同級由左而右。',`−${p*n}÷${p}=−${n}。`);
   if(k===5)return make(u,l,`${tag}存款 ${n*10} 元，支出 ${p*10} 元後又存入 ${p*5} 元，餘額為何？`,n*10-p*5,[n*10-p*10,n*10+p*5,n*10],'依事件順序加減。',`${n*10}−${p*10}+${p*5}=${n*10-p*5}。`);
   if(k===6)return make(u,l,`${tag}哪一式的值等於 ${n-p}？`,`${n}+(−${p})`,[`${n}−(−${p})`,`−${n}+${p}`,`−${n}−${p}`],'加負數等同減正數。',`${n}+(−${p})=${n-p}。`);
   if(k===7)return make(u,l,`${tag}計算 −[${n}−(${p}−${n})]。`,-(2*n-p),[2*n-p,-p,p],'先括號，再處理最外負號。',`−(${2*n-p})=${-(2*n-p)}。`);
   if(k===8)return make(u,l,`${tag}連續 ${p} 天每天虧損 ${n} 元，以整數表示總變化。`,-n*p,[n*p,-n-p,n-p],'虧損用負數。',`(−${n})×${p}=−${n*p}。`);
   return make(u,l,`${tag}若 a=−${n}、b=${p}，a−2b 為何？`,-n-2*p,[-n+2*p,n-2*p,-n-p],'代入後依序運算。',`−${n}−${2*p}=${-n-2*p}。`);
  }
  if(u==='分配律與運算規則'){
   if(k===3)return make(u,l,`${tag}${n}×${p}+${n}×${p+1} 可提出的共同因數為何？`,n,[p,n+p,2*n],'兩項都有相同乘數。',`提出 ${n}。`);
   if(k===4)return make(u,l,`${tag}${n}×99 最適合改寫成哪一式？`,`${n}×(100−1)`,[`${n}×(100+1)`,`99×(${n}−1)`,`${n}×100−1`],'利用接近整百的數。',`${n}×100−${n}。`);
   if(k===5)return make(u,l,`${tag}(−${n})×(${p}−3) 展開為何？`,`−${n*p}+${3*n}`,[`−${n*p}−${3*n}`,`−${n*p}+3`,`−${n}×${p}−3`],'負數也要乘到每一項。',`−${n*p}+${3*n}。`);
   if(k===6)return make(u,l,`${tag}${n}×${p}+${n}×(10−${p}) 的值為何？`,10*n,[n*p,10*n-p,n*10+p],'先提出共同因數。',`${n}×10=${10*n}。`);
   if(k===7)return make(u,l,`${tag}長方形長 ${n}+${p}、寬 ${n}，面積展開式為何？`,`${n}×${n}+${n}×${p}`,[`${n}+${p}×${n}`,`${n*n}+${p}`,`${n+p+n}`],'面積是長乘寬。',`${n}(${n}+${p})。`);
   if(k===8)return make(u,l,`${tag}哪一式與 ${n}×(${p}−1) 不相等？`,`${n*p}−1`,[`${n*p}−${n}`,`${n}×${p}−${n}`,`${n}×(${p}−1)`],'括號內每一項都要乘。',`${n*p}−1 少減了 ${n-1}。`);
   return make(u,l,`${tag}計算 ${p}×${n}+${p}×(20−${n})。`,20*p,[p*n,20*p-n,p*(20+n)],'提出共同因數。',`${p}×20=${20*p}。`);
  }
  if(u==='指數記法與科學記號'){
   if(k===3)return make(u,l,`${tag}10^${p}×10^3 可寫成何者？`,`10^${p+3}`,[`10^${p*3}`,`20^${p+3}`,`10^${p-3}`],'同底數相乘，指數相加。',`10^${p+3}。`);
   if(k===4)return make(u,l,`${tag}(${p}×10^${n%5+3})÷10^2 為何？`,`${p}×10^${n%5+1}`,[`${p}×10^${n%5+5}`,`${p/10}×10^${n%5+3}`,`${p}×10^2`],'除以 10²，指數減 2。',`指數為 ${n%5+1}。`);
   if(k===5)return make(u,l,`${tag}科學記號 0.${p}×10^${n%4+4} 的係數不合規定，正確寫法為何？`,`${p}×10^${n%4+3}`,[`${p}×10^${n%4+4}`,`${p*10}×10^${n%4+3}`,`0.${p}×10^${n%4+3}`],'係數須介於 1 與 10。',`小數點右移一位，指數減 1。`);
   if(k===6)return make(u,l,`${tag}2^3×2^${p}÷2^2=2^m，m 為何？`,p+1,[p+3,p-1,2*p],'乘法加指數、除法減指數。',`m=3+${p}−2=${p+1}。`);
   if(k===7)return make(u,l,`${tag}下列何者最大？`,`9×10^${p}`,[`8×10^${p}`,`7×10^${p}`,`6×10^${p}`],'指數相同時比較係數。','係數 9 最大。');
   if(k===8)return make(u,l,`${tag}${p}×10^${n%4+2} 展開後末尾有幾個 0？`,n%4+2,[n%4+1,n%4+3,p],'整數係數後接指數個 0。',`共有 ${n%4+2} 個 0。`);
   return make(u,l,`${tag}(3^${p})² 可寫成何者？`,`3^${2*p}`,[`3^${p+2}`,`6^${p}`,`9^${2*p}`],'冪的乘方，指數相乘。',`(3^${p})²=3^${2*p}。`);
  }
  if(u==='因數與倍數'){
   const a=2*n,b=3*n;
   if(k===3)return make(u,l,`${tag}${a} 與 ${b} 的最大公因數為何？`,n,[2*n,3*n,6*n],'找共同質因數。',`最大公因數為 ${n}。`);
   if(k===4)return make(u,l,`${tag}將 ${n*p} 顆糖平均分成 ${p} 袋，每袋幾顆？`,n,[p,n*p,n+p],'總數除以袋數。',`${n*p}÷${p}=${n}。`);
   if(k===5)return make(u,l,`${tag}甲燈每 ${p} 秒亮一次，乙燈每 ${p+2} 秒亮一次，首次同時亮需找什麼？`,'最小公倍數',['最大公因數','兩數相加','兩數相減'],'週期再次重合使用最小公倍數。','找兩週期的最小公倍數。');
   if(k===6)return make(u,l,`${tag}${n*p} 的因數成對出現，已知其中一個是 ${p}，配對因數為何？`,n,[p,n+p,n*p],'兩因數相乘等於原數。',`${n*p}÷${p}=${n}。`);
   if(k===7)return make(u,l,`${tag}長 ${n*p} 公分木條等分且每段為整數公分，下列何者可能是每段長？`,p,[p+1,n*p+1,n+p],'每段長必須整除總長。',`${p} 是 ${n*p} 的因數。`);
   if(k===8)return make(u,l,`${tag}兩數最大公因數為 ${p}，下列何者一定正確？`,'兩數都是 p 的倍數',['兩數相加等於 p','兩數互質','兩數都小於 p'],'公因數能整除兩數。',`兩數皆可寫成 ${p} 的倍數。`);
   return make(u,l,`${tag}${a} 與 ${b} 的最小公倍數和最大公因數相乘為何？`,a*b,[6*n,6*n*n,a+b],'兩數乘積等於最大公因數乘最小公倍數。',`${a}×${b}=${a*b}。`);
  }
  if(u==='倍數判別與質數'){
   if(k===3)return make(u,l,`${tag}數字 4${p}2 若為 3 的倍數，${p} 應符合哪項條件？`,`${p+6} 是 3 的倍數`,[`${p} 是偶數`,`${p+4} 是 9 的倍數`,`${p} 必為 0`],'各位數和需為 3 的倍數。',`4+${p}+2=${p+6}。`);
   if(k===4)return make(u,l,`${tag}下列哪一個是合數？`,n%2?21:25,[17,19,23],'合數有兩個以上的非 1 正因數。','21 或 25 可分解。');
   if(k===5)return make(u,l,`${tag}2、3、5、${n%2?7:11} 的乘積共有幾種不同質因數？`,4,[3,5,n],'計算不同質數的個數。','共有四種。');
   if(k===6)return make(u,l,`${tag}下列哪組數互質？`,'8 與 15',['8 與 12','9 與 15','10 與 25'],'互質的最大公因數為 1。','8 與 15 沒有共同質因數。');
   if(k===7)return make(u,l,`${tag}${n%2?135:225} 是否為 5 的倍數？`,'是，個位數為 5',['否，位數和不是 5','是，因為是奇數','無法判斷'],'個位數為 0 或 5。','符合 5 的倍數判別。');
   if(k===8)return make(u,l,`${tag}質數 p 的正因數個數為何？`,2,[1,p,p+1],'質數只有 1 與本身。','共有 2 個。');
   return make(u,l,`${tag}若 ${p}×${n} 為偶數，且 ${p} 為偶數，對 ${n} 可確定什麼？`,'可能奇也可能偶',['一定是奇數','一定是偶數','一定是質數'],'有一個因數為偶數，乘積即為偶數。','無法決定另一因數奇偶。');
  }
  if(u==='一元一次方程式'){
   const x=p,a=2+n%5,b=n,c=a*x+b;
   if(k===3)return make(u,l,`${tag}${a}x=${a*x}，x 為何？`,x,[a*x,x+1,a],'等式兩邊同除以係數。',`x=${x}。`);
   if(k===4)return make(u,l,`${tag}方程式 x−${n}=${p} 的解為何？`,n+p,[n-p,p-n,n],'兩邊同加 ${n}。',`x=${n+p}。`);
   if(k===5)return make(u,l,`${tag}某數的 ${a} 倍再加 ${b} 得 ${c}，某數為何？`,x,[c-b,c/a,x+1],'依文字列方程式。',`${a}x+${b}=${c}。`);
   if(k===6)return make(u,l,`${tag}${a}(x+${p})=${a*(n+p)}，x 為何？`,n,[n+p,n-p,a*n],'先同除以 ${a}。',`x+${p}=${n+p}，x=${n}。`);
   if(k===7)return make(u,l,`${tag}連續兩整數和為 ${2*n+1}，較小者為何？`,n,[n+1,2*n,2*n+1],'設較小者 x，另一數 x+1。',`2x+1=${2*n+1}。`);
   if(k===8)return make(u,l,`${tag}長方形長比寬多 ${p}，周長 ${4*n+2*p}，寬為何？`,n,[n+p,2*n+p,4*n+2*p],'周長為兩倍的長寬和。',`2[x+(x+${p})]=${4*n+2*p}。`);
   return make(u,l,`${tag}方程式 ${a}x+${b}=${a}x+${b} 有多少個解？`,'無限多個',['無解','只有一解','兩個解'],'兩邊完全相同。','所有數代入都成立。');
  }
  if(u==='一元一次不等式'){
   if(k===3)return make(u,l,`${tag}x>${n} 的最小整數解為何？`,n+1,[n,n-1,n+2],'大於不包含等於。',`最小整數為 ${n+1}。`);
   if(k===4)return make(u,l,`${tag}−x≤−${n}，x 的範圍為何？`,`x≥${n}`,[`x≤${n}`,`x>${n}`,`x<${n}`],'乘除負數時不等號反向。',`x≥${n}。`);
   if(k===5)return make(u,l,`${tag}${p}x<${p*n}，下列哪個數是解？`,n-1,[n,n+1,n+2],'兩邊同除以正數。',`x<${n}。`);
   if(k===6)return make(u,l,`${tag}年齡 x 未滿 ${n} 歲，應寫成何者？`,`x<${n}`,[`x≤${n}`,`x>${n}`,`x≥${n}`],'未滿不包含界線。',`x<${n}。`);
   if(k===7)return make(u,l,`${tag}${p}x+2≥${p*n+2}，最小整數解為何？`,n,[n-1,n+1,p*n],'先減 2，再除正數。',`x≥${n}。`);
   if(k===8)return make(u,l,`${tag}整數 x 同時滿足 ${n-3}<x≤${n}，共有幾個解？`,3,[2,4,n],'列出界線間整數。',`${n-2}、${n-1}、${n}。`);
   return make(u,l,`${tag}某班至少要 ${n} 人參加，已有 ${p} 人，還需 x 人，列式為何？`,`${p}+x≥${n}`,[`${p}+x>${n}`,`${p}x≥${n}`,`x−${p}≥${n}`],'總參加人數至少達標。',`${p}+x≥${n}。`);
  }
  if(u==='比例與比值'){
   if(k===3)return make(u,l,`${tag}${p}:${n}=${2*p}:x，x 為何？`,2*n,[n/2,n+p,p*n],'內外項交叉相乘。',`x=${2*n}。`);
   if(k===4)return make(u,l,`${tag}${p} 杯米配 ${2*p} 杯水，米水比為何？`,'1:2',['2:1',`${p}:${p}`,`${p}:2`],'同比例約分。','得到 1:2。');
   if(k===5)return make(u,l,`${tag}單價 ${p*10} 元，買 ${n} 件共多少元？`,p*10*n,[p*10+n,p*n,p*10],'正比：總價等於單價乘數量。',`${p*10}×${n}=${p*10*n}。`);
   if(k===6)return make(u,l,`${tag}比例尺 1:${n*1000}，圖上 2 公分代表實際幾公尺？`,20*n,[2*n,200*n,n],'先算公分再換公尺。',`2×${n*1000}÷100=${20*n}。`);
   if(k===7)return make(u,l,`${tag}甲乙分錢比 3:2，共 ${5*n} 元，甲得多少？`,3*n,[2*n,5*n,n],'總份數 5，甲占 3 份。',`${5*n}÷5×3=${3*n}。`);
   if(k===8)return make(u,l,`${tag}${n} 公里用時 ${p} 小時，平均時速為何？`,n/p,[n*p,n+p,p/n],'速率等於距離除以時間。',`${n}÷${p}=${n/p}。`);
   return make(u,l,`${tag}若 y 與 x 成正比，x 加倍時 y 如何變化？`,'加倍',['減半','不變','增加固定 1','變為 0'],'正比的比值固定。','x 加倍，y 也加倍。');
  }
  if(u==='工作率與反比'){
   if(k===3)return make(u,l,`${tag}機器每小時完成 ${n} 件，${p} 小時完成幾件？`,n*p,[n+p,n/p,n],'工作量等於效率乘時間。',`${n}×${p}=${n*p}。`);
   if(k===4)return make(u,l,`${tag}完成 ${n*p} 件需 ${p} 小時，平均每小時幾件？`,n,[p,n*p,n+p],'效率等於工作量除以時間。',`${n*p}÷${p}=${n}。`);
   if(k===5)return make(u,l,`${tag}甲單獨 ${p} 天完成，每天完成全部工作的幾分之幾？`,`1/${p}`,[`${p}/1`,`1/${p+1}`,`${p}%`],'把全部工作視為 1。',`工作率為 1/${p}。`);
   if(k===6)return make(u,l,`${tag}${p} 人做 ${n} 天，改成 ${2*p} 人需幾天？`,n/2,[n,2*n,p*n],'工作量固定，人數加倍時間減半。',`${p}×${n}÷${2*p}=${n/2}。`);
   if(k===7)return make(u,l,`${tag}水龍頭每分鐘注水 ${p} 公升，水箱尚缺 ${n*p} 公升，需幾分鐘？`,n,[p,n*p,n+p],'時間等於工作量除以速率。',`${n*p}÷${p}=${n}。`);
   if(k===8)return make(u,l,`${tag}路程固定，速度變為原來 2 倍，所需時間如何？`,'變為一半',['變為兩倍','不變','增加 2 小時','無法判斷'],'速度與時間成反比。','乘積保持固定。');
   return make(u,l,`${tag}甲每小時做 ${n} 件、乙每小時做 ${p} 件，合作 1 小時共做幾件？`,n+p,[n*p,n-p,n],'工作率可以相加。',`${n}+${p}=${n+p}。`);
  }
  if(u==='坐標與線型函數'){
   if(k===3)return make(u,l,`${tag}點 (${n},−${p}) 位於哪一象限？`,'第四象限',['第一象限','第二象限','第三象限'],'x 正、y 負。','位於第四象限。');
   if(k===4)return make(u,l,`${tag}點 (${n},${p}) 對 y 軸對稱後為何？`,`(−${n},${p})`,[`(${n},−${p})`,`(−${n},−${p})`,`(${p},${n})`],'對 y 軸對稱只改 x 符號。',`(−${n},${p})。`);
   if(k===5)return make(u,l,`${tag}函數 y=${p}x，當 y=${p*n} 時 x 為何？`,n,[p,n*p,n+p],'反向以 y 除以係數。',`${p*n}÷${p}=${n}。`);
   if(k===6)return make(u,l,`${tag}直線 y=${p}x+${n} 的 y 軸截距為何？`,n,[p,-n,0],'令 x=0。',`y=${n}。`);
   if(k===7)return make(u,l,`${tag}下列哪一點在 y=2x+1 上？`,`(${n},${2*n+1})`,[`(${n},${2*n})`,`(${n+1},${2*n+1})`,`(${2*n+1},${n})`],'將 x 代入檢查 y。',`2×${n}+1=${2*n+1}。`);
   if(k===8)return make(u,l,`${tag}點 A(${n},${p}) 向左 ${p} 單位後為何？`,`(${n-p},${p})`,[`(${n+p},${p})`,`(${n},0)`,`(${n},${2*p})`],'向左使 x 減少。',`x=${n}−${p}。`);
   return make(u,l,`${tag}若 x 坐標為 0，該點一定位於何處？`,'y 軸上',['x 軸上','第一象限','原點'],'y 可為任意數。','x=0 的點在 y 軸。');
  }
  if(u==='資料分析'){
   if(k===3)return make(u,l,`${tag}資料 ${n}、${n}、${p}、${n} 的眾數為何？`,n,[p,(3*n+p)/4,n-p],'出現次數最多者是眾數。',`${n} 出現 3 次。`);
   if(k===4)return make(u,l,`${tag}五筆資料平均為 ${n}，總和為何？`,5*n,[n,4*n,n+5],'總和等於平均乘筆數。',`${n}×5=${5*n}。`);
   if(k===5)return make(u,l,`${tag}四次成績 ${n}、${n+2}、${n+4}、x，平均 ${n+3}，x 為何？`,n+6,[n+3,n+5,4*n+12],'先求應有總和。',`${4*(n+3)}−${3*n+6}=${n+6}。`);
   if(k===6)return make(u,l,`${tag}資料最大值 ${n+p}、最小值 ${p}，全距為何？`,n,[n+p,p,n-p],'全距等於最大減最小。',`${n+p}−${p}=${n}。`);
   if(k===7)return make(u,l,`${tag}次數分配表中某數出現 ${p} 次，總資料 ${n+p} 筆，其相對次數為何？`,`${p}/${n+p}`,[`${n}/${n+p}`,`${p}/${n}`,`${n+p}/${p}`],'相對次數等於次數除以總數。',`${p}/${n+p}。`);
   if(k===8)return make(u,l,`${tag}加入一個比原平均大的資料後，平均數通常如何？`,'上升',['下降','不變','變成 0'],'新資料高於原平均會拉高平均。','平均數上升。');
   return make(u,l,`${tag}資料 ${n-2}、${n}、${n+2}、${n+4} 的中位數為何？`,n+1,[n,n+2,n-2],'偶數筆取中間兩數平均。',`${n} 與 ${n+2} 的平均為 ${n+1}。`);
  }
  const total=n*10,used=p*5;
  if(k===3)return make(u,l,`${tag}閱讀紀錄：週一 ${n} 頁、週二 ${p} 頁、週三 ${n+p} 頁，共讀幾頁？`,2*n+2*p,[n+p,2*n+p,n+2*p],'把三天頁數相加。',`${n}+${p}+${n+p}=${2*n+2*p}。`);
  if(k===4)return make(u,l,`${tag}公車 8:00 出發，行駛 ${n+20} 分鐘，到站時間為何？`,`8:${String(n+20).padStart(2,'0')}`,[`8:${String(n).padStart(2,'0')}`,`9:00`,`8:20`],'起始時間加行駛時間。',`8:00 加 ${n+20} 分鐘。`);
  if(k===5)return make(u,l,`${tag}班級票選中甲 ${n} 票、乙 ${p} 票、丙 ${n+p} 票，最高票是誰？`,'丙',['甲','乙','甲乙並列'],'比較三個票數。',`丙有 ${n+p} 票。`);
  if(k===6)return make(u,l,`${tag}儲值 ${total} 元，搭車使用 ${used} 元，餘額為何？`,total-used,[total+used,used,total],'餘額等於原有減使用。',`${total}−${used}=${total-used}。`);
  if(k===7)return make(u,l,`${tag}活動限額 ${n+p} 人，已報名 ${n} 人，還可報名幾人？`,p,[n,n+p,n-p],'限額減已報名。',`${n+p}−${n}=${p}。`);
  if(k===8)return make(u,l,`${tag}包裝標示每份 ${p} 公克，共 ${n} 份，總重多少公克？`,n*p,[n+p,n/p,n],'讀取單位後相乘。',`${p}×${n}=${n*p}。`);
  return make(u,l,`${tag}兩方案：甲固定 ${n} 元再加每次 ${p} 元；乙每次 ${p+2} 元。使用 ${n} 次，甲費用為何？`,n+n*p,[n*(p+2),n+p,n*p],'依方案文字建立算式。',`${n}+${n}×${p}=${n+n*p}。`);
 };
})();
